{
  "name": "banana-logger",
  "version": "0.1.0",
  "description": "Banana (logger): A fun and advanced TypeScript logger with formatting, callback, and timing capabilities",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "browser": "./dist/index.browser.js",
  "types": "./dist/index.d.ts",
  "type": "module",
  "exports": {
    ".": {
      "browser": {
        "import": "./dist/index.browser.js",
        "types": "./dist/index.browser.d.ts"
      },
      "bun": {
        "import": "./dist/index.js",
        "types": "./dist/index.d.ts"
      },
      "node": {
        "import": "./dist/index.js",
        "types": "./dist/index.d.ts"
      },
      "default": {
        "import": "./dist/index.js",
        "types": "./dist/index.d.ts"
      }
    }
  },
  "scripts": {
    "build:server": "bun build ./src/index.ts --outdir=dist -e pino* --format esm --sourcemap=linked --minify",
    "build:browser": "bun build ./src/index.browser.ts --outdir=dist --format esm --sourcemap=linked --minify",
    "build:ci": "node ./bun.config.js",
    "build:ts": "tsc --project tsconfig.json --pretty",
    "build": "bun run clean && bun run build:server && bun run build:browser && bun run build:ts",
    "clean": "rm -rf dist",
    "clean:modules": "rm -fr node_modules",
    "clean:deep": "bun run clean && bun run clean:modules && bun install && bun run build:bun",
    "examples": "bun ./examples.ts",
    "lint": "biome check ./src",
    "prepublishOnly": "bun run build",
    "test": "bun test --coverage",
    "test:ci": "bun run lint && bun test"
  },
  "keywords": [
    "logger",
    "typescript",
    "pino",
    "logging"
  ],
  "author": "Marco Trevisani <marco.trevisani81@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "pino": "^10.3.1",
    "pino-pretty": "^13.1.3"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.14",
    "@types/bun": "^1.3.9",
    "@types/node": "^25.2.3",
    "esbuild": "^0.27.3",
    "glob": "13.0.2",
    "typescript": "^5.9.3"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "packageManager": "bun@1.3.9",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/trevonerd/banana.git"
  },
  "bugs": {
    "url": "https://github.com/trevonerd/banana/issues"
  },
  "homepage": "https://github.com/trevonerd/banana#readme"
}
